<div class="cycle">
	<div class="cycle-title-group">
		<h2>
			<%= cycle.title %>
		</h2>
		<%= erb :_cycle_dates, locals: {cycle: cycle} %>
	</div>
	<% cycle.items.each do |item| %>
		<%= erb :_item, locals: {item: item} %>
	<% end %>
	<a href="/items/new<%= "?roadmap_id=#{@roadmap.id}&cycle_id=#{cycle.id}" %>">
Add something
	</a>
</div>